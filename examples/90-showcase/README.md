# Showcase

End-to-end demos that combine flow matching with delta alignment on
image-scale backbones (e.g. reproducing a full few-shot anatomical landmark
detection pretraining run). Add tiered examples here as the library grows;
keep `00-foundations/`, `10-sampling/`, and `20-training/` focused on single
concepts.
